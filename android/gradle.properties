org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
org.gradle.parallel=true
org.gradle.caching=true
# Let Flutter manage the Java runtime (uses the Flutter-provided JBR). If needed, set org.gradle.java.home via local properties.

# Force Gradle to use JDK 21 to avoid 'Unsupported class file major version 68' (Java 24)
# Adjust this path if your JDK 21 is installed elsewhere.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.7.6-hotspot
